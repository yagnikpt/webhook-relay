version: 2

before:
  hooks:
    - go mod tidy

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    main: ./cmd/whrelay
    binary: whrelay

archives:
  - formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        formats: [zip]

homebrew_casks:
  - name: whrelay
    description: "A lightweight, self-hosted webhook relay server."
    homepage: "https://github.com/yagnikpt/webhook-relay"
    repository:
      owner: yagnikpt
      name: homebrew-tap
    hooks:
      post:
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/whrelay"]
          end

nfpms:
  - maintainer: Yagnik Patel <yagnik.pt@gmail.com>
    description: A lightweight, self-hosted webhook relay server.
    homepage: https://github.com/yagnikpt/webhook-relay
    license: MIT
    formats:
      - deb
      - rpm

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
